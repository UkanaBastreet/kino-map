import { FC } from "react";
import { IMovieItem } from "../types";

interface MovieListProps {
  movies?: IMovieItem;
}

export const MovieList: FC = ({}) => {
  return (
    <>
      <div>{}</div>
    </>
  );
};
